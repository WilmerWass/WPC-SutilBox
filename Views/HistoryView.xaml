<UserControl x:Class="Wpc_SutilBox.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="800">
    <Grid Style="{StaticResource AnimatedViewStyle}">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20">
                <TextBlock Text="📋 Historial de Acciones y Seguridad" Style="{StaticResource HeaderTextStyle}" Margin="0,0,0,10"/>
                <TextBlock Text="Registro de cambios realizados, trazabilidad y puntos de restauración." Style="{StaticResource SubHeaderTextStyle}" Margin="0,0,0,25"/>

                <Border Background="{DynamicResource SurfaceBrush}" CornerRadius="12" Padding="20" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="Registros de Sesión" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,10"/>
                        <TextBlock Text="Actividad reciente de esta sesión." FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,15"/>
                        <ListBox ItemsSource="{Binding RecentLogEntries}" Height="300" Background="Transparent" BorderThickness="0" Foreground="{DynamicResource TextSecondaryBrush}" FontFamily="Consolas" FontSize="11" Margin="0,0,0,15"/>
                        <Button Content="Actualizar registros" Command="{Binding RefreshLogsCommand}" Style="{StaticResource SecondaryButtonStyle}" HorizontalAlignment="Left" Margin="0,0,0,8"/>
                        <Button Content="Abrir Carpeta de Logs" Command="{Binding OpenLogFolderCommand}" Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Left"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
