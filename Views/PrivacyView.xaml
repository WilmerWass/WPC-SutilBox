<UserControl x:Class="Wpc_SutilBox.Views.PrivacyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="800">
    <Grid Style="{StaticResource AnimatedViewStyle}">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20">
                <TextBlock Text="🔒 Control de Privacidad y Telemetría" Style="{StaticResource HeaderTextStyle}" Margin="0,0,0,10"/>
                <TextBlock Text="Administre la privacidad de Windows, servicios de telemetría y diagnósticos." Style="{StaticResource SubHeaderTextStyle}" Margin="0,0,0,25"/>

                <Border Background="{DynamicResource SurfaceBrush}" CornerRadius="12" Padding="20" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="Configuración de Telemetría de Windows" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,15"/>

                        <Grid Margin="0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Desactivar Telemetría del Sistema" FontWeight="Medium" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <TextBlock Text="Reduce el envío de datos de diagnóstico a Microsoft." FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}"/>
                            </StackPanel>
                            <CheckBox Grid.Column="1" VerticalAlignment="Center" IsChecked="True"/>
                        </Grid>

                        <Separator Background="{DynamicResource BorderSubtleBrush}" Margin="0,12"/>

                        <Grid Margin="0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Desactivar Rastreo de Experiencia del Usuario (DiagTrack)" FontWeight="Medium" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <TextBlock Text="Detiene y deshabilita el servicio de seguimiento de diagnósticos." FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}"/>
                            </StackPanel>
                            <CheckBox Grid.Column="1" VerticalAlignment="Center" IsChecked="True"/>
                        </Grid>

                        <Separator Background="{DynamicResource BorderSubtleBrush}" Margin="0,12"/>

                        <Grid Margin="0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Desactivar ID de Anuncios" FontWeight="Medium" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <TextBlock Text="Evita que las aplicaciones usen el ID de publicidad para mostrar anuncios personalizados." FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}"/>
                            </StackPanel>
                            <CheckBox Grid.Column="1" VerticalAlignment="Center" IsChecked="True"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Background="{DynamicResource SurfaceBrush}" CornerRadius="12" Padding="20">
                    <StackPanel>
                        <TextBlock Text="Telemetría de WPC SUTILBOX" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,10"/>
                        <TextBlock Text="WPC SUTILBOX respeta plenamente su privacidad. Ningún dato personal es recopilado ni enviado a servidores externos." FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,0,0,15"/>
                        <Border Background="{DynamicResource SurfaceElevatedBrush}" CornerRadius="8" Padding="12">
                            <TextBlock Text="✓ Privacidad Local Garantizada. Todos los análisis e historiales permanecen únicamente en este equipo." FontSize="11" Foreground="{DynamicResource PrimaryBrush}" FontWeight="SemiBold"/>
                        </Border>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
