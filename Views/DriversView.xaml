<UserControl x:Class="Wpc_SutilBox.Views.DriversView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="800">
    <Grid Style="{StaticResource AnimatedViewStyle}">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20">
                <TextBlock Text="🧩 Administración de Drivers y Controladores" Style="{StaticResource HeaderTextStyle}" Margin="0,0,0,10"/>
                <TextBlock Text="Diagnóstico de controladores del sistema y accesos a herramientas de fabricante." Style="{StaticResource SubHeaderTextStyle}" Margin="0,0,0,25"/>

                <Border Background="{DynamicResource SurfaceBrush}" CornerRadius="12" Padding="20" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="Administrador de Dispositivos" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,10"/>
                        <TextBlock Text="Examine los dispositivos instalados y sus controladores mediante la consola oficial de Windows." FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,15"/>
                        <Button Content="Abrir devmgmt.msc" Command="{Binding RunToolCommand}" CommandParameter="devmgmt.msc" Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Left"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
