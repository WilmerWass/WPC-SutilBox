<UserControl x:Class="Wpc_SutilBox.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer Style="{StaticResource AnimatedViewStyle}">
        <StackPanel Margin="20">
            <TextBlock Text="Configuración del Sistema" Style="{StaticResource HeaderTextStyle}" Margin="0,0,0,10"/>
            <TextBlock Text="Personaliza el comportamiento y la apariencia de Wpc_SutilBox" Style="{StaticResource SubHeaderTextStyle}" Margin="0,0,0,30"/>

            <!-- Herramientas Avanzadas -->
            <Border Style="{StaticResource AnimatedCardStyle}" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Herramientas Avanzadas" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryBrush}" Margin="0,0,0,20"/>
                    <TextBlock Text="Configuración detallada de rendimiento y perfiles." Foreground="#888" FontSize="12" Margin="0,0,0,15"/>
                    <Button Content="⚙️ Abrir Editor de Perfiles" Command="{Binding NavigateCommand}" CommandParameter="EditorPerfiles" Style="{StaticResource SecondaryButtonStyle}" HorizontalAlignment="Left" Padding="20,10"/>
                </StackPanel>
            </Border>

            <!-- Comportamiento -->
            <Border Style="{StaticResource AnimatedCardStyle}" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Comportamiento del Sistema" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryBrush}" Margin="0,0,0,20"/>
                    
                    <Grid Margin="0,0,0,15">
                        <StackPanel>
                            <TextBlock Text="Ejecutar al iniciar Windows" Foreground="White" FontSize="15" Margin="0,0,0,5"/>
                            <TextBlock Text="Inicia la protección y el monitoreo automáticamente." Foreground="#888" FontSize="12"/>
                        </StackPanel>
                        <ToggleButton HorizontalAlignment="Right" IsChecked="{Binding RunOnStartup}" Style="{StaticResource ToggleSwitchStyle}"/>
                    </Grid>

                    <Separator Background="#222" Margin="0,15"/>

                    <Grid Margin="0,0,0,15">
                        <StackPanel>
                            <TextBlock Text="Optimizar cuando no uso el PC (Idle)" Foreground="White" FontSize="15" Margin="0,0,0,5"/>
                            <TextBlock Text="Ejecuta limpieza y optimización automática tras 10 min de inactividad." Foreground="#888" FontSize="12"/>
                        </StackPanel>
                        <ToggleButton HorizontalAlignment="Right" IsChecked="{Binding OptimizeOnIdle}" Style="{StaticResource ToggleSwitchStyle}"/>
                    </Grid>

                    <Separator Background="#222" Margin="0,15"/>

                    <Grid>
                        <StackPanel>
                            <TextBlock Text="Minimizar a la bandeja" Foreground="White" FontSize="15" Margin="0,0,0,5"/>
                            <TextBlock Text="Al cerrar la ventana, la aplicación seguirá en segundo plano." Foreground="#888" FontSize="12"/>
                        </StackPanel>
                        <ToggleButton HorizontalAlignment="Right" IsChecked="{Binding MinimizeToTray}" Style="{StaticResource ToggleSwitchStyle}"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Personalización -->
            <Border Style="{StaticResource AnimatedCardStyle}" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Apariencia y Estilo" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryBrush}" Margin="0,0,0,20"/>
                    
                    <Grid Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="Modo Oscuro" Foreground="White" FontSize="15" Margin="0,0,0,5"/>
                            <TextBlock Text="Alterna entre tema claro y oscuro (Recomendado: Oscuro)." Foreground="#888" FontSize="12"/>
                        </StackPanel>
                        <ToggleButton HorizontalAlignment="Right" IsChecked="{Binding IsDarkMode}" Style="{StaticResource ToggleSwitchStyle}"/>
                    </Grid>

                    <TextBlock Text="Color del Tema" Foreground="White" FontSize="15" Margin="0,0,0,15"/>
                    <WrapPanel Orientation="Horizontal" Margin="0,0,0,25">
                        <!-- Azul (Default) -->
                        <Button Command="{Binding ChangeAccentColorCommand}" CommandParameter="#3B82F6" Width="30" Height="30" Margin="0,0,10,10" ToolTip="Azul Sky">
                            <Button.Template><ControlTemplate TargetType="Button"><Border Background="#3B82F6" CornerRadius="15"/></ControlTemplate></Button.Template>
                        </Button>
                        <!-- Verde Esmeralda -->
                        <Button Command="{Binding ChangeAccentColorCommand}" CommandParameter="#10B981" Width="30" Height="30" Margin="0,0,10,10" ToolTip="Verde Esmeralda">
                            <Button.Template><ControlTemplate TargetType="Button"><Border Background="#10B981" CornerRadius="15"/></ControlTemplate></Button.Template>
                        </Button>
                        <!-- Rojo Carmesí -->
                        <Button Command="{Binding ChangeAccentColorCommand}" CommandParameter="#EF4444" Width="30" Height="30" Margin="0,0,10,10" ToolTip="Rojo Carmesí">
                            <Button.Template><ControlTemplate TargetType="Button"><Border Background="#EF4444" CornerRadius="15"/></ControlTemplate></Button.Template>
                        </Button>
                        <!-- Púrpura Real -->
                        <Button Command="{Binding ChangeAccentColorCommand}" CommandParameter="#8B5CF6" Width="30" Height="30" Margin="0,0,10,10" ToolTip="Púrpura Real">
                            <Button.Template><ControlTemplate TargetType="Button"><Border Background="#8B5CF6" CornerRadius="15"/></ControlTemplate></Button.Template>
                        </Button>
                        <!-- Naranja Atardecer -->
                        <Button Command="{Binding ChangeAccentColorCommand}" CommandParameter="#F97316" Width="30" Height="30" Margin="0,0,10,10" ToolTip="Naranja Atardecer">
                            <Button.Template><ControlTemplate TargetType="Button"><Border Background="#F97316" CornerRadius="15"/></ControlTemplate></Button.Template>
                        </Button>
                        <!-- Rosa Neón -->
                        <Button Command="{Binding ChangeAccentColorCommand}" CommandParameter="#EC4899" Width="30" Height="30" Margin="0,0,10,10" ToolTip="Rosa Neón">
                            <Button.Template><ControlTemplate TargetType="Button"><Border Background="#EC4899" CornerRadius="15"/></ControlTemplate></Button.Template>
                        </Button>
                        <!-- Cyan Tecnológico -->
                        <Button Command="{Binding ChangeAccentColorCommand}" CommandParameter="#06B6D4" Width="30" Height="30" Margin="0,0,10,10" ToolTip="Cyan Tech">
                            <Button.Template><ControlTemplate TargetType="Button"><Border Background="#06B6D4" CornerRadius="15"/></ControlTemplate></Button.Template>
                        </Button>
                        <!-- Lima Eléctrica -->
                        <Button Command="{Binding ChangeAccentColorCommand}" CommandParameter="#84CC16" Width="30" Height="30" Margin="0,0,10,10" ToolTip="Lima Eléctrica">
                            <Button.Template><ControlTemplate TargetType="Button"><Border Background="#84CC16" CornerRadius="15"/></ControlTemplate></Button.Template>
                        </Button>
                        <!-- Ámbar Elegante -->
                        <Button Command="{Binding ChangeAccentColorCommand}" CommandParameter="#F59E0B" Width="30" Height="30" Margin="0,0,10,10" ToolTip="Ámbar">
                            <Button.Template><ControlTemplate TargetType="Button"><Border Background="#F59E0B" CornerRadius="15"/></ControlTemplate></Button.Template>
                        </Button>
                        <!-- Indigo Profundo -->
                        <Button Command="{Binding ChangeAccentColorCommand}" CommandParameter="#6366F1" Width="30" Height="30" Margin="0,0,10,10" ToolTip="Indigo">
                            <Button.Template><ControlTemplate TargetType="Button"><Border Background="#6366F1" CornerRadius="15"/></ControlTemplate></Button.Template>
                        </Button>
                        
                        <!-- Custom Hex Input -->
                         <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0,0,10">
                            <TextBlock Text="#" Foreground="#888" VerticalAlignment="Center" Margin="0,0,5,0" FontSize="14"/>
                            <TextBox x:Name="HexInput" Text="" Width="80" Height="30" VerticalContentAlignment="Center" Padding="5,0" Background="#222" Foreground="White" BorderThickness="1" BorderBrush="#444"/>
                            <Button Content="Aplicar" Command="{Binding ChangeAccentColorCommand}" CommandParameter="{Binding Text, ElementName=HexInput}" Style="{StaticResource SecondaryButtonStyle}" Margin="5,0,0,0" Height="30" Padding="10,0"/>
                        </StackPanel>
                    </WrapPanel>

                    <Separator Background="#222" Margin="0,15"/>

                    <Grid>
                        <StackPanel>
                            <TextBlock Text="Idioma de la Interfaz" Foreground="White" FontSize="15" Margin="0,0,0,5"/>
                            <TextBlock Text="Selecciona el idioma preferido para la aplicación." Foreground="#888" FontSize="12"/>
                        </StackPanel>
                        <ComboBox HorizontalAlignment="Right" SelectedValue="{Binding SelectedLanguage}" SelectedValuePath="Tag" Width="150">
                            <ComboBoxItem Content="Español (ES)" Tag="es"/>
                            <ComboBoxItem Content="English (EN)" Tag="en"/>
                            <ComboBoxItem Content="Português (PT)" Tag="pt"/>
                        </ComboBox>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Soporte y Logs -->
            <Border Style="{StaticResource AnimatedCardStyle}">
                <StackPanel>
                    <TextBlock Text="Soporte y Diagnóstico" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryBrush}" Margin="0,0,0,20"/>
                    <TextBlock Text="Si experimentas problemas, puedes revisar los registros de actividad." Foreground="#888" FontSize="12" Margin="0,0,0,15"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                        <Button Content="💬 Unirse a Discord" Command="{Binding OpenDiscordCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,10,0" Padding="20,10" Background="#5865F2" BorderBrush="#5865F2"/>
                        <Button Content="📂 Abrir Carpeta de Logs" Command="{Binding OpenLogFolderCommand}" Style="{StaticResource SecondaryButtonStyle}" Padding="20,10"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
