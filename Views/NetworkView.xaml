<UserControl x:Class="Wpc_SutilBox.Views.NetworkView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="800">
    <Grid Style="{StaticResource AnimatedViewStyle}">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20">
                <TextBlock Text="🌐 Diagnóstico y Herramientas de Red" Style="{StaticResource HeaderTextStyle}" Margin="0,0,0,10"/>
                <TextBlock Text="Estado de adaptadores de red, limpieza de caché DNS y herramientas TCP/IP." Style="{StaticResource SubHeaderTextStyle}" Margin="0,0,0,25"/>

                <UniformGrid Columns="2" Margin="0,0,0,20">
                    <Border Background="{DynamicResource SurfaceBrush}" CornerRadius="12" Padding="20" Margin="0,0,10,0">
                        <StackPanel>
                            <TextBlock Text="Limpieza de DNS" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,10"/>
                            <TextBlock Text="Vacía la caché de resolución DNS de Windows para solucionar problemas de conectividad." FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,15" TextWrapping="Wrap"/>
                            <Button Content="Ejecutar ipconfig /flushdns" Command="{Binding RunToolCommand}" CommandParameter="ipconfig /flushdns" Style="{StaticResource PrimaryButtonStyle}"/>
                        </StackPanel>
                    </Border>

                    <Border Background="{DynamicResource SurfaceBrush}" CornerRadius="12" Padding="20" Margin="10,0,0,0">
                        <StackPanel>
                            <TextBlock Text="Restablecer Winsock y TCP/IP" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,10"/>
                            <TextBlock Text="Restablece el catálogo del protocolo de red a su configuración inicial." FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,15" TextWrapping="Wrap"/>
                            <Button Content="Restablecer Red" Command="{Binding RunToolCommand}" CommandParameter="netsh winsock reset" Style="{StaticResource SecondaryButtonStyle}"/>
                        </StackPanel>
                    </Border>
                </UniformGrid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
